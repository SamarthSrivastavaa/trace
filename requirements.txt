# Core (required). The full 422-test suite runs on these alone, offline.
pydantic>=2.0
pytest>=8.0

# OPTIONAL - only for the live proposer and the kill test.
# The offline suite and the demo run without it.
#   pip install anthropic
# Version present at the phase3-freeze measurement:
#   anthropic==1.3.0
